<%= form_with(model: huntx, local: true) do |form| %>
  <% if huntx.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(huntx.errors.count, "error") %> prohibited this clue from being saved:</h2>
      <ul>
        <% huntx.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title %><br>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
